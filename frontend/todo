# TODO — Sidebar tree cho STOCK
# Đã push qua backend team để confirm folder structure.

# Option đề xuất: Inventory làm folder cha, bên trong có:
#   - Tồn kho (hiện tại)
#   - Nhập kho
#   - Xuất kho
#   - (sau này) Kiểm kê
#   - (sau này) Điều chỉnh

# Search/Filter product khi thêm vào phiếu nhập/xuất
# Hiện tại: dropdown Select đổ hết products, user lặn lội tìm thủ công
# Cần: combobox có search (gõ tên/sku) + filter theo brand/category
# Ví dụ: dùng shadcn Command + Popover để làm searchable select
# Ảnh hưởng: import-create-page.tsx, export-create-page.tsx
